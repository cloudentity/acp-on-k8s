apiVersion: v1
kind: Service
metadata:
  name: acp-faas-nodejs-v4
  labels:
    environmentName: nodejs-v4
spec:
  selector:
    environmentName: nodejs-v4
  ports:
    - name: http-env
      protocol: TCP
      port: 8888
      targetPort: http-env
