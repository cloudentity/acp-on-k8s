version: "1.1"
name: Canary Test Suite
env:
  acp_base_url: https://acp-canary.acp.svc.cluster.local:8443
  tid: ${tid}
  aid: ${aid}
  client_id: lightweight-tests-client-id
include:
  - auth_with_userinfo.yml
