apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cert-manager
spec:
  values:
    prometheus:
      enabled: true
      servicemonitor:
        enabled: true
        interval: ""
